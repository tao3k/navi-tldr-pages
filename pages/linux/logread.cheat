; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/logread.md

% logread, linux

# Print the log
logread

# Print `n` messages
logread -l <n>

# Filter messages by (Keyword/`regex`)
logread -e <pattern>

# Print log messages as they happen
logread -f

# Display help
logread -h
