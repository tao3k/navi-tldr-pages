; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/mke2fs.md

% mke2fs, linux

# Create an ext2 filesystem in partition 1 of device b (`sdb1`)
mke2fs -t ext2 <dev_sdb1>

# Create an ext3 filesystem in partition 1 of device b (`sdb1`)
mke2fs -t ext3 <dev_sdb1>

# Create an ext4 filesystem in partition 1 of device b (`sdb1`)
mke2fs -t ext4 <dev_sdb1>
