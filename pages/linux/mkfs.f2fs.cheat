; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/mkfs.f2fs.md

% mkfs.f2fs, linux

# Create an F2FS filesystem inside partition Y on device X
sudo mkfs.f2fs <dev_sdXY>

# Create an F2FS filesystem with a volume label
sudo mkfs.f2fs -l <volume_label> <dev_sdXY>
