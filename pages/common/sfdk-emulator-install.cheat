; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/sfdk-emulator-install.md

% sfdk-emulator-install, common

# Install an emulator
sfdk emulator install <name>

# Remove an emulator
sfdk emulator remove <name>

# Display the available emulators
sfdk emulator list <[-a|--available]>

# Display the installed emulators
sfdk emulator list
