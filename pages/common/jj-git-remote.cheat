; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/jj-git-remote.md

% jj-git-remote, common

# List all Git remotes
jj git remote list

# Add a Git remote
jj git remote add <remote> <url>

# Change the URL of a Git remote
jj git remote set-url <remote> <url>

# Remove a Git remote
jj git remote remove <remote>

# Rename a Git remote
jj git remote rename <old_name> <new_name>
